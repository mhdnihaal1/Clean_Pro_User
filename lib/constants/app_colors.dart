import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryBlue = Color(0xFF40C4FF);
  static const Color secondaryBlue = Color(0xFF2196F3);

  // Background colors
  static const Color backgroundWhite = Colors.white;
  static const Color backgroundGrey = Color.fromARGB(255, 240, 240, 240);

  // Text colors
  static const Color textBlack = Colors.black;
  static const Color textGrey = Color(0xFF757575);
  static const Color textLightGrey = Color(0xFFBDBDBD);

  // Action colors
  static const Color actionBlue = Color(0xFF40C4FF);
  static const Color actionGreen = Color(0xFF4CAF50);
  static const Color actionRed = Color(0xFFF44336);

  // Icon colors
  static const Color iconBlue = Color(0xFF40C4FF);
  static const Color iconGrey = Color(0xFF757575);

  // Border colors
  static const Color borderGrey = Color(0xFFE0E0E0);

  // Status colors
  static const Color statusConfirmed = Color(0xFF4CAF50);
  static const Color statusPending = Color(0xFFFFC107);
  static const Color statusCancelled = Color(0xFFF44336);

  // Button colors
  static const Color buttonBlue = Color(0xFF40C4FF);
  static const Color buttonDisabled = Color(0xFFBDBDBD);

  // Shimmer effect colors (for loading states)
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);

  // Transparent colors
  static const Color transparent = Colors.transparent;
  static const Color semiTransparentBlack = Color(0x80000000);

  // Gradient colors
  static const List<Color> blueGradient = [
    Color(0xFF40C4FF),
    Color(0xFF2196F3),
  ];
}
